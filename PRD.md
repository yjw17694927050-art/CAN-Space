# CAN-Space — 产品需求文档 / Product Requirements Document (PRD)

| 项 Item | 值 Value |
|---|---|
| **产品名 Product** | CAN-Space |
| **文档版本 Version** | 1.0 |
| **日期 Date** | 2026-09-11 |
| **状态 Status** | 草案 Draft（待评审 / pending review） |
| **文档语言 Language** | 中英对照（中文为主） |
| **作用域 Scope** | 个人化改造 + 商业化探索的第一阶段规划 |

---

## 0. 开发溯源声明 / Provenance Declaration

> **本产品 CAN-Space 是基于开源项目 CAN-Space 二次开发（fork + 个人化改造）的个人工具。**
>
> - **上游上游（Upstream）**：CAN-Space — CAN Bus Reverse-Engineering Workstation
>   原作者（Author）：Sherin Joseph Roy
>   上游仓库（Upstream repo）：https://github.com/Sherin-SEF-AI/CAN-Space
>   上游许可证（License）：MIT
>   - 项目根 README（本目录 README.md）保留了上游的完整功能说明、安全警告与致谢，作为功能溯源与合规依据。
> - **本产品仓库（This repo）**：https://github.com/yjw17694927050-art/CAN（本地目录名：`CAN-Space`）
> - **与上游的关系**：在保留上游全部功能与安全机制（ARM TX 安全门、首次启动声明、只读 UDS 默认等）的前提下，针对**中文支持、界面简化、国产大模型接入、个人化工作流与商业化方向**进行二次开发。
> - **许可证义务**：因为上游为 MIT，本项目的二次开发与分发须保留上游版权声明与许可证文本（见 LICENSE）。任何派生代码在使用/分发时均需注明基于 Upstream: CAN-Space 开发。

---

## 1. 背景与动机 / Background & Motivation

### 1.1 现状

CAN-Space（上游）已经是一个功能完整、包含 15 个标签页、覆盖采集/回放/分析/AI/诊断/注入的 CAN 总线逆向工程工作站。经过一次完整代码审查和一批修复（见 ROADMAP 与 README 中 Recent fixes），项目运行稳定、测试通过。

### 1.2 为什么做个人化

原工具面向通用逆向工程师，功能多、界面专业，但存在三个对"个人工具"不友好的问题：

1. **语言门槛**：界面与 AI 提示词为英文，且 AI 提示词硬编码了特定车型（Hyundai/Kona），换车型即失效。
2. **功能过载**：15 个标签页平铺在单个窗口，常用功能（采集/回放/表格/实时信号）与专业功能（注入/诊断/网关）混杂，学习成本高。
3. **模型锁定**：AI 只支持 Anthropic / Groq / Ollama，与国内开发者常用的国产大模型生态脱节。

### 1.3 目标

把 CAN-Space 从"强大的通用工具"改造成"**属于一个人的、简单顺手、接中国生态的 CAN 逆向工作台**"，并为未来的商业化留下一块可验证的根基。

---

## 2. 用户与场景 / Users & Use Cases

| 用户 User | 场景 Scenario | 核心需求 Core Need |
|---|---|---|
| 个人（本项目的目标用户） | 个人逆向车辆 CAN 数据、制作 DBC、写代码生成 | 快、顺手、中文、不开箱即用一堆不相关的专业 tab |
| 未来潜在客户（若商业化） | 车企/供应商/维修店的测试工程师 | 可靠、可复现、可交付（证据链、标准格式导出） |

> **阶段定位**：本 PRD 聚焦"个人工具"；商业化作为独立阶段（ROADMAP Phase 4），不占用当前开发主要时间。

---

## 3. 需求规格 / Requirements

### R1 — 中文支持 / Chinese Support

**优先级 Priority**: P0（地基）

**需求描述**：
- R1.1 界面全部文本支持中文（15 个标签页 + 面板 + 设置对话框 + 错误提示）。
- R1.2 建立统一的 i18n 语言包机制（参考 Qt `QTranslator`/`tr()` 或自建轻量语言包），**第一版只出一套中文**，但机制允许后期加其它语言。
- R1.3 AI 引擎支持中文解读：重写/参数化 `SYSTEM_PROMPT`，可切换"英文/中文"输出。
- R1.4 抽离 AI 提示词中的车型硬编码，改为可配置的"车型知识包"。

**验收标准**：
- 一个开关即可在"中/英"界面之间切换（或至少全中文可用）。
- AI 输出能按所选语言生成结构化解读。
- 换车型不再需要改代码。

**工期估算（参考）**：1～2 周（含文案提取、机制搭建、AI 提示词重构与测试）。

---

### R2 — 界面更简单易用 / UI Simplification

**优先级 Priority**: P0

**需求描述**：
- R2.1 采用**精简主界面 + 高级模式**（推荐）：主界面只保留采集、回放、帧表格、实时信号 4 块核心；注入、诊断、网关、模糊、脚本等进阶标签页收入"高级模式"折叠，默认隐藏。
- R2.2 对每个保留的 tab 做"默认即可用"优化：去掉过深的默认选项，常用操作放首屏。
- R2.3 首次使用提供一次性引导（可选），说明打卡/采集/分析/出 DBC 的最小流程。

**验收标准**：
- 新用户 5 分钟内能完成"加载样例 → 看到帧表格 → 出 DBC"三个核心动作。
- 主编界面元素数量相比现状明显减少。
- 所有原功能仍可从"高级模式"访问（不删功能，只藏功能）。

**工期估算（参考）**：2～4 周（取决于简化深度；推荐先做方案 A：折叠高阶）。

---

### R3 — 国产大模型接入 / Domestic LLM Support

**优先级 Priority**: P1（收益确定、技术难度低）

**需求描述**：
- R3.1 新增一个通用 **OpenAI 兼容 provider 层** `_run_openai_compatible()`，一次接入即可复用多家。
- R3.2 首批接入（建议顺序）：
  - 通义千问（阿里 DashScope）
  - DeepSeek
  - Kimi（Moonshot）
  - 智谱 GLM
  - 豆包 / 火山方舟（可选）
  - 本地 Qwen（通过已有 Ollama 支持，零成本）
- R3.3 设置界面支持为每个国产 provider 单独填 base_url / api_key / model。
- R3.4 保持 Anthropic / Groq 兼容（老用户不破坏）。

**验收标准**：
- 至少 3 家国产模型可用并有配置界面。
- OpenAI 兼容 provider 一次配置可切换多家。
- 现有测试全部通过，无回归。

**工期估算（参考）**：1～2 周（通用兼容层 2～3 天 + 每家接线 0.5～1 天 + 配置界面与测试）。

---

### R4 — 商业化方向 / Commercialization Direction

**优先级 Priority**: P2（长周期，独立阶段）

**需求描述与决策项（此小节为方向性，不锁定）**：
- R4.1 采用 **开放核心（Open Core）** 为主路径：
  - **免费开源**：核心采集、回放、分析、DBC 生成（保持现能力）。
  - **收费**：AI 云解读额度、云 DBC 信号库、团队协作、企业级诊断协议包、技术支持。
- R4.2 探索 **企业/顾问服务**：软件白送，靠帮车企/供应商搭建逆向产线、数据标注、方案咨询收费（个人最容易落地）。
- R4.3 长期可选项：硬件绑定套件（软件 + 采集硬件/线束打包）、SaaS 云端分析。

**商业化前置条件（Gate）**：
- 必须先完成：去车型锁定（R1.4）、可靠性与可信度基建（证据链/时间戳/总线状态）、标准格式导出、教程与文档。
- 未满足前置条件前，不投入商业化正式开发。

**合规要点（需自行核实 / to verify）**：逆向、注入、SecurityAccess 爆破等功能在不同司法辖区的知识产权与安全法规风险不同；商业化展示/售卖范围需提前评估。**本文档不提供法律结论，请咨询专业人士。**

**工期估算（参考）**：MVP（license/授权/安装包/付费入口）1～2 周；商业模式真正验证 3～6 个月（依赖用户基数，非纯开发工时）。

---

## 4. 非目标 / Non-Goals（本期范围内不做）

- 实时 ECU 刷写/标定。
- 车型特定调参。
- 生产级（OEM 认证级）汽车诊断，除非商业化进入企业阶段。
- 移动端/Web 端（列入长期路线，本期不做）。
- 全量简化到"删除高级功能"（保留功能，只做收纳）。

---

## 5. 影响范围 / Impact & Boundaries

| 模块 Module | 影响 Impact |
|---|---|
| `core/safety.py` | 不变（安全门必须完全保留，所有 TX 路径继续强制 `require_armed()`） |
| `core/ai_client.py` | 重构：加 OpenAI 兼容 provider 抽象层；SYSTEM_PROMPT 参数化 |
| `canlab/tabs/*` + `mainwindow.py` | 重构主窗口 tab 容器支持"精简/高级"模式；tabs 文案抽取 i18n |
| `settings_dialog.py` | 增加国产 provider 配置；接入语言设置 |
| `core/state.py` | 不变（内存上限、分段存储保持） |
| `main.py` | 首次启动引导（可选） |
| 全部 TX 路径 | 强制保留 ARM TX 安全门，不得因 UI 简化而移除 |

---

## 6. 非功能需求 / Non-Functional Requirements

- **安全**：任何界面/功能改动不得削弱 ARM TX 安全门、首次启动声明、只读 UDS 默认等机制。
- **可维护性**：i18n 语言包与 AI provider 均以配置/注册机制实现，避免硬编码。
- **兼容性**：保持与上游功能对齐，避免破坏已有分析能力；老配置（API key、DBC）兼容。
- **测试**：每项需求附回归测试（延续现有 `tests/` 约定，期望基线约 148 passed / 2 skipped 保持）。

---

## 7. 里程碑与验收总览 / Milestones at a Glance

（详细排期见 ROADMAP.md）

| 阶段 Phase | 内容 | 工期（估算） |
|---|---|---|
| P1 地基 | 去车型锁定、通用 AI provider 层、i18n 基建 | 2～3 周 |
| P2 体验 | 界面简化（方案 A）、国产模型接入、可信度基建 | 3～4 周 |
| P3 数据资产 | 会话存档、信号指纹库、多 ECU 关联 | 3～4 周 |
| P4 商业化验证 | 开放核心 + 企业服务试点 | 3～6 个月（独立） |

---

## 8. 风险与开放问题 / Risks & Open Questions

| 风险/问题 | 说明 | 处理 |
|---|---|---|
| 合规风险（优先级高） | 逆向/注入/爆破的商业化展示范围有法律风险 | 前置评估，商业化前咨询专业人士 |
| 车型锁定 | AI 提示词硬编码 Hyundai/Kona | P1 去锁定，改为车型知识包 |
| 界面简化返工 | 若先做中文后做简化，文案提取两次 | 基建先行，本 PRD 已把 i18n 放 P1 |
| 商业验证周期 | 依赖用户基数，非纯开发 | 先积累用户与数据资产，再投商业化 |
| 是否需要官方 Windows 安装包 | 当前 run-from-source | 商业化前置，列入 Phase 4 决策 |

---

## 9. 文档关联 / Related Documents

- **AGENT.md** — 开发与协作指南（编码规范、架构约定、安全规则）
- **SPEC.md** — 技术规格说明书（架构、数据模型、协议、性能）
- **ROADMAP.md** — 阶段化路线图与里程碑（本 PRD 的排期落地）
- **README.md** — 功能总览、安全警告、启动方式（保留上游溯源）
- **docs/AUDIT_FIXES.md** — 已修复缺陷清单

---

*PRD 完 / End of PRD*