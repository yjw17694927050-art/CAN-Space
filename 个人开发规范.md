# 个人开发规范 / Personal Development Guidelines

> **版本 Version**：1.0
> **日期 Date**：2026-09-10
> **对象 Owner**：CAN-Space 项目个人开发者
> **语言 Language**：中文（命令与技术术语保留英文）

本文档独立于 `SPEC.md`（技术规格）。它只写「*我* 该怎么开发、提交、测试」，是个人化的工程约定。随实践迭代，反复推翻的约定要及时删改。

---

## 1. 提交前门槛 / Pre-Commit Gate

无论改动多小，提交前必须跑一次全量测试，全绿才提交：

```bash
.venv\Scripts\python.exe -m pytest tests/ -q
```

- 预期结果：**169 passed, 1 skipped**（UDS 硬件依赖跳过）。
- 出现失败先定位根因，禁止用 `--no-verify` / 注释跳过掩盖问题。
- 新增或修改 UI 标签页后，`tests/test_gui_tabs.py` 要有对应交互用例，再改代码。

## 2. Git 提交规范 / Git Commit Conventions

### 2.1 仓库与身份 / Repo & Identity

- 远端固定为：`https://github.com/yjw17694927050-art/CAN.git`
- 提交身份：`user.name=YJW`，`user.email=yjw17694927050@gmail.com`
- 不要误推到旧地址 `YJWCAN.git`。

### 2.2 提交信息 / Commit Message

统一语义化前缀，中英皆可，但保持一致：

| 前缀 Prefix | 用途 Use for |
|-------------|--------------|
| `fix:` | 缺陷修复、回归修复 |
| `feat:` | 新功能 |
| `test:` | 测试新增/调整 |
| `refactor:` | 不改行为的重构 |
| `docs:` | 文档 |

示例：`fix: 修正 MI 归一化对数底，消除 30% 系统性低估`

### 2.3 提交纪律 / Discipline

- 一个提交只做一件事（功能/修复/测试分开）；相关小改动可合并。
- 提交前 `git status` 核对，按文件名 `git add`，避免 `git add -A` 误带敏感文件。
- 不提交 `.env`、凭据、`node_modules/`、`__pycache__/`（已由 `.gitignore` 兜底）。

### 2.4 分支约定 / Branch Convention

- `main`：稳定基线，只接受来自 `dev`/`feature/*` 的合入，不直接在其上堆开发。
- `dev`：日常开发默认分支，功能起步点；个人单作者项目可只维护 `main` + `dev`。
- `feature/*`：较大或做完可能丢弃的功能提交到功能分支，合回 `dev` 后删除。
- 开发流程：`dev` 上随做随提交 → 每阶段退出标准达成后合并到 `main` 打基线 → 及时同步 ROADMAP 勾选。
- 提交前仍必须过第 1 节全量测试门槛。

## 3. 代码工程质量 / Code Quality

以下为反复踩坑沉淀的硬性约束，改动时勿破坏：

- **ARM 发射安全门**：所有总线写帧路径（injection/replay/gateway/uds/rest_api 等）在 `bus.send()` 前必须调用 `require_armed()` 显式布防。
- **表达式沙箱**：安全访问（seed→key）等用户可控表达式只走 `ast` 白名单解析，禁止 `eval()`。
- **内存上限**：`core/state.py` 的 `max_frames`（默认 500_000）用于防止长抓取 OOM，不要随意移除；如需更长抓取先评估内存。
- **线程归属**：`QThread` 实例必须保存在 self 上（Worker 的性质），防止局部变量被 GC 提前销毁导致 "QThread: Destroyed while running"。
- **信号连接**：连接全局单例（如 `AppState`）的信号时用绑定方法而非 lambda——lambda 无接收者上下文，Qt 在所属控件销毁后不会自动断开，重载帧会操作已删除控件。
- **ID 统一**：CAN ID 一律经 `core.canid.normalize_id` 规范化为大写、去 `0x`、至少三位十六进制，禁止用 `str.lstrip("0x")` 误削字符。

## 4. 界面开发约定 / UI Conventions

- 各标签页保持"硬件/线程惰性创建"：不要在任何标签页 `__init__` 里启动实机采集或线程，放入具体操作方法内。
- 实时刷新（如 PLOT 每帧重算）必须节流，避免饿死 GUI 事件循环。
- `MainWindow.closeEvent` 调用 `_stop_tab_workers()` join 全部标签页线程后再退出。
- 界面测试与被测代码统一通过顶层 `core.*` 导入，避免 `canlab.core.*` 与 `core.*` 形成两个独立单例。

## 5. 文档化 / Documentation

- 项目根 `SPEC.md`（技术规格）与 `个人开发规范.md`（本文）分离：架构与功能进其中一份，个人开发纪律进本文。
- 逻辑优化与缺陷修复，在对应测试文件补一条回归用例，并在提交信息中说明根因。
- 本文件的版本随约定迭代递增，每次实质变更标注日期。

---

*规范文档结束 / End of guidelines*